# Set this when building for production (your server URL, no trailing slash).
# Example: https://kirworld-server.onrender.com
# Leave empty for local dev (uses same origin + Vite proxy).
VITE_WS_URL=
